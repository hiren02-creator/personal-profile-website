# Shared UI Components

This project currently uses a single React `App` component and has no separate shared UI primitive directory. Buttons, cards, inputs, tags, navigation, and other interface patterns are authored directly in `frontend/src/App.jsx` and styled in `frontend/src/App.css`.

No standalone shared components are available to include here.

